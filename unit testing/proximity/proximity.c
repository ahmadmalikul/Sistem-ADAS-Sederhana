#include "Proximity.h"
#include "HCSR.h" // agar bisa pakai distance dan fungsi HCSR

void Proximity_CheckAndActivate() {
    if (distance < 100.0f) {
        // Misalnya: nyalakan buzzer atau tampilkan warning
        // Simulasi fungsi logika proximity
    }
}
